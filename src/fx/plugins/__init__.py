"""
Compatibility wrappers for the pre-refactor fx plugin modules.

Built-in commands are composed explicitly from :mod:`fx.command_sets`; importing
modules in this package no longer mutates the public command registry.
"""

